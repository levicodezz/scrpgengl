services:
  - type: web
    name: h5p-auth
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: JWT_SECRET
        value: "coloque_um_segredo_forte_aqui"
